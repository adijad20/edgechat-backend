# Optional: copy to .env.test to override test defaults.
# Tests use postgres:postgres@localhost:5432/postgres and mongodb://localhost:27017 by default.
# Redis default is localhost:6379. If using this repo's docker-compose (Redis on host port 16379), set:
# REDIS_URL=redis://localhost:16379/0
