# EdgeChat Backend — copy to .env and fill in. Do not commit .env.
# Windows: copy .env.example .env

# --- Required ---

# PostgreSQL (async driver). For Docker: postgresql+asyncpg://postgres:postgres@postgres:5432/postgres
DATABASE_URL=postgresql+asyncpg://postgres:secret@localhost:5432/smartlensdb

# MongoDB. For Docker: mongodb://mongodb:27017
MONGODB_URL=mongodb://localhost:27017

# Redis. For Docker: redis://redis:6379/0  (host often uses 16379 on Windows)
REDIS_URL=redis://localhost:16379

# Auth — use a long random string in production
JWT_SECRET=your-secret-at-least-32-chars-long-change-in-prod

# --- Optional ---

# Google Gemini; required for /ai/complete and chat AI replies
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# App
APP_NAME=EdgeChat Backend

# CORS: comma-separated origins, or * for all
CORS_ORIGINS=*

# Rate limit per IP
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW_SECONDS=60

# JWT (optional overrides)
JWT_ALGORITHM=HS256
JWT_ACCESS_EXPIRE_MINUTES=15
JWT_REFRESH_EXPIRE_DAYS=7
