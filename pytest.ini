# Day 5 — Pytest config. Session-scoped event loop so asyncpg/SQLAlchemy share one loop (avoids "another operation is in progress").
[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests
pythonpath = .
